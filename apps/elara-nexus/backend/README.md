# Elara Nexus Backend

FastAPI service for boards/tasks, chat sessions, and memory search.

## Commands

```bash
uv sync --all-groups
uv run uvicorn app.main:app --reload
uv run ruff check .
uv run mypy app
uv run pytest
```
